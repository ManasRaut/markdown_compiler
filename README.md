# Markdown Compiler

A simple markdown compiler that can convert the markdown files into html files. 

> Do not use it in producution as it is only made for learning purposes.

## Examples

Examples can be found in `examples` folder.